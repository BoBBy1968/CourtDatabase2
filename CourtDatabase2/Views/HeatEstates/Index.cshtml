@model IEnumerable<CourtDatabase2.Data.Models.HeatEstate>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AbNumber)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AbNumber)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AbNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AbNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AbNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
